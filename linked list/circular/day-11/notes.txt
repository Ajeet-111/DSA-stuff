it works like singly linked list except that the last node stores the address for first node.
widely used when cycle questions are a thing in the assessment.
instead of head, we keep the track of the last node of the linked list.
